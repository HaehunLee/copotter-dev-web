import styled from "@emotion/styled";

const Template = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;

  padding: 12px;
`;

const x = {
  Template,
};

export default x;
